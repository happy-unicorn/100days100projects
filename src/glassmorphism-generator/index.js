import React from 'react';
import styled from 'styled-components';

const Page = styled.div``;

const GlassmorphismGeneratorPage = (props) => {
    return (
        <Page>

        </Page>
    );
};

export default GlassmorphismGeneratorPage;