import React from 'react';
import styled from 'styled-components';

const Page = styled.div`
  height: 100%;
`;

const BeautifulInputPage = (props) => {
    return (
        <Page>
            хе хе
        </Page>
    );
};

export default BeautifulInputPage;